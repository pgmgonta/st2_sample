---
description: Test Action
enabled: true
entry_point: test.sh
name: test 
parameters:
  hosts:
    default: "{{ action_context.hosts }}"
  username: 
    default: "{{ action_context.username }}"
  private_key: 
    default: "{{ action_context.private_key }}"
  param:
    type: "string"
    position: 0
    required: true
    default: "{{ action_context.param}}"
runner_type: "remote-shell-cmd"
