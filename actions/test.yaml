---
description: Test Action
enabled: true
entry_point: test.sh
name: test 
parameters:
  hosts:
    default: "{{ args.hosts }}"
  username: 
    default: "{{ args.username }}"
  private_key: 
    default: "{{ args.private_key }}"
  param:
    type: "string"
    position: 0
    required: true
    default: "{{ args.param}}"
runner_type: "remote-shell-cmd"
